AgentRouter + Claude Code

🚀 Get $175 in AgentRouter promotional credits
Register with the referral link [https://agentrouter.org](https://agentrouter.org/register?aff=TNCH)

This page is an independent reference for developers looking for information about AgentRouter with Claude Code, Anthropic-compatible APIs, AI coding agents, and custom API endpoints.

AgentRouter API Endpoint

The AgentRouter API endpoint for Claude Models is:

https://agentrouter.org/

This endpoint is used for API requests and compatible AI coding integrations.

AgentRouter documentation: https://agentrouter.org/docs/index.html

AgentRouter + Claude Code

AgentRouter can be used with AI coding workflows that support custom API providers or Anthropic-compatible API configurations.

When configuring Claude Code with AgentRouter, use the API endpoint supported by AgentRouter's current documentation:

https://agentrouter.org/

The exact Claude Code configuration can vary depending on the Claude Code version and the compatibility method being used.

Always check the current documentation before configuring an integration.

Anthropic-Compatible API

AgentRouter documents compatibility with Anthropic-style API workflows.

When using an Anthropic-compatible integration, follow the environment variables, authentication method, endpoint format, and model configuration documented by AgentRouter.

Do not assume that configuration settings from an older Claude Code version will remain unchanged.

Model Selection

When using Claude Code with AgentRouter, use a model identifier that AgentRouter currently supports.

Model availability and identifiers can change over time.

Before configuring a model, check AgentRouter's current documentation for:

Supported models
Model identifiers
Context limits
Pricing
Availability
API compatibility
API Key Security

Keep your AgentRouter API key private.

Never:

Commit API keys to public GitHub repositories
Put API keys in public source code
Share API keys in screenshots
Post API keys in GitHub issues
Store API keys in client-side applications

If an API key is exposed, revoke or rotate it as soon as possible.

Troubleshooting
Authentication Problems

If Claude Code cannot authenticate, check:

The API key is valid.
The API key is active.
The configured endpoint is correct.
Your Claude Code version supports the configuration method being used.
Unsupported Model

If Claude Code reports that a model is unavailable, check AgentRouter's current model list and verify the exact model identifier.

Endpoint Configuration

The AgentRouter API endpoint is:

https://agentrouter.org/

The registration/referral URL is separate:

[agentrouter.org](https://agentrouter.org/register?aff=TNCH)

Do not use the registration URL as an API endpoint.

Registration

Eligible users can review the current AgentRouter service here:

[agentrouter.org](https://agentrouter.org/register?aff=TNCH)

The registration link contains a referral parameter.

Official Resources
AgentRouter: [https://agentrouter.org](https://agentrouter.org/register?aff=TNCH)
API: https://agentrouter.org/
Documentation: https://agentrouter.org/docs/index.html
Related References
AgentRouter API
AgentRouter Promotional Funds
AgentRouter + Codex

Disclosure: This is an independent community reference and is not the official AgentRouter repository. The registration link above contains a referral parameter.
